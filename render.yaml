services:
  - type: web
    name: fastapi-food-insecurity
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: MODEL_PATH
        value: modele_food_insecurity.pkl
